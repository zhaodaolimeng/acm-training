#include <bits/stdc++.h>

using namespace std;

const int max_np = 105;
int NP, NT, t;

vector<int> tokens;
vector<int> transitions;
map<int, int> p2t;

int main(){
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);
    

    cin>>NP;
    for(int i=0; i<NP; i++){
        cin>>t;
        tokens.push_back(t);
    }

    cin>>NT;
    for(int i=0; i<NT; i++){
        while(cin>>t && t != 0){
            
        }
    }
}
